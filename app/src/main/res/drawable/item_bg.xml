<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" /> <!-- Màu nền của shape -->
    <corners android:radius="30dp" /> <!-- Bán kính cong viền -->
    <stroke
        android:width="5dp"
        android:color="@color/primary" /> <!-- Độ rộng viền -->
</shape>